import { BlockExplainer } from "./BlockExplainer";
import { hooksBlocksSpec } from "./specs/hooks-blocks";

export const HooksBlockExplainer: React.FC = () => (
  <BlockExplainer spec={hooksBlocksSpec} />
);
