---
description: "[Moved] Web research has moved to ctk. Use /ctk:web-research instead."
---

# web-research

This command has moved to **ctk** where the web-research-analyst agent lives.

Use: `/ctk:web-research $ARGUMENTS`

Or if ctk is installed (recommended), just use `/web-research` and it will resolve from there.
